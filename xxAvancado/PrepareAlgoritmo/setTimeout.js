/* O método setTimeout define um tempo que executara uma função ou 
parte de um código assim que o tempo expirar */

setTimeout(() => {
    console.log('Time Out Executado')
}, 3000) // Retorna Time Out Executado ** Depois de 3 segundos **